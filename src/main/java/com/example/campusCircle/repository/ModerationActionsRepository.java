package com.example.campusCircle.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.example.campusCircle.model.ModerationActions;

public interface ModerationActionsRepository extends JpaRepository<ModerationActions, Long> { }
