package com.example.campusCircle;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * Integration test for application context loading.
 * Disabled because it requires full MongoDB and external services setup.
 * Unit tests for services are in the service package.
 */
@Disabled("Integration test requires full MongoDB and external services setup")
class CampusCircleApplicationTests {

	@Test
	void contextLoads() {
		// This test is disabled - see class annotation
	}

}
